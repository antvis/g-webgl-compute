import { mat4, quat, vec3 } from 'gl-matrix';
import { CAMERA_TYPE, CameraComponent } from '../CameraComponent';

describe('Camera component', () => {
  test('should rotate correctly.', () => {
    // const component = new CameraComponent(CAMERA_TYPE.EXPLORING);
    // component.setPerspective();
    // component.rotate(quat.create());
    // component.updateTransform();
    // expect(component.getRotation()).toStrictEqual(quat.create());
  });
});
