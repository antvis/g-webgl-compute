// @ts-nocheck
import { IRenderEngine } from '@antv/g-webgpu-core';
import { injectable } from 'inversify';

// @ts-ignore
@injectable()
export class WebGLEngine implements IRenderEngine {
  public beginFrame() {
    //
  }

  public endFrame() {
    //
  }
}
